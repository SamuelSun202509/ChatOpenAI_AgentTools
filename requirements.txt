# ===== SAP Cloud SDK for AI (包含 gen_ai_hub) =====
sap-ai-sdk-gen[all]

# ===== LangChain 生态 =====
langchain~=1.2.6
langchain-community~=0.4.1
langchain-openai~=1.1.0
langchain-aws~=1.1.0
langgraph>=1.0.0

# ===== SAP HANA Vector Store =====
langchain-hana>=0.2.0
hdbcli>=2.21.28

# ===== Web UI =====
streamlit>=1.32.0

# ===== Tools =====
langchain-tavily>=0.2.0
# `tools/weather.py` calls Open-Meteo via httpx; declared explicitly so we
# don't silently rely on it being a transitive dep of langchain-openai.
httpx>=0.27.0
# `tools/wikipedia.py` calls the MediaWiki API directly via `requests`
# (also a transitive dep of langchain). We deliberately do NOT depend on
# `wikipedia-api`, because that library uses an httpx Client that ignores
# corporate-proxy env vars on Windows.
requests>=2.31.0

# ===== Utilities =====
python-dotenv>=1.0.0

# ===== XSUAA / JWT validation (used in app.py via auth_service) =====
sap-xssec>=4.1.0
PyJWT>=2.8.0
